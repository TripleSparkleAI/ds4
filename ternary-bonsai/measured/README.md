# MEASURED - what we have actually measured, copied in whole with its provenance

> ✅ **Everything in this directory is a real measurement of a real file on a real box.** It is the
> only directory in `ternary-bonsai/` that is not a plan.

Copied unmodified from `prism/` on branch `autonomous/dspark-concurrency-and-spark-queue`, 2026-09-20.

| file | what it holds |
|---|---|
| `ISSUE_1085_bonsai-style-ternary-ptq-for-v41-flash_what-we-measured_2026-09-20.md` | ★ **the origin of this whole folder.** The engram byte census, the type trap, the three router facts, the prepared comment, and the P0-to-P4 plan. |
| `MEASURED_DECODEPROFILE_one-flag-recovered-the-decode-and-the-kernel-prismstall-characterised-fires-zero-times-in-it_2026-09-19.md` | the decode kernel mix behind the CUDA graph on the GB10; where the `<142>` shares come from |
| `PREREG_DECODEPROFILE_01_the-decode-kernel-mix-behind-the-cuda-graph_2026-09-19.md` | that measurement's sealed pre-registration, committed before the run |

## ⚠ THE ISSUE FILE IS THE ORIGIN AND THE REST OF THE FOLDER IS ITS EXPANSION

`../methods/`, `../explore/` and `../wiki/` expand the plan and the findings in that file. **Where they would
disagree, that file wins and the expansion is wrong and must be fixed.** Two plans for one job
disagree eventually, and the one a reader happens to open becomes the one that gets built.

## ⛔ THE BANNER ON THE ISSUE FILE IS LIVE AND TRAVELS WITH THE COPY

Its first line reads *"NOT SUBMITTED. Nothing here has been posted, commented or pushed anywhere."*
**That is still true.** The prepared comment in its `## Body` section has not been posted to issue
1085 or anywhere else. Copying the file into this folder did not change its status, and this
paragraph exists so that a reader who meets the copy first does not assume otherwise.

## ⚠ Every figure in here is checkpoint-attached and box-attached

The byte census is a census of **our V4.1 Flash Q2 gguf**, read on the **rented RTX 5090** (clock
UTC+0000). The decode profile is the **GB10** (clock UTC+10). ⛔ **A figure does not transfer to
another file or another box by being restated**, and the folder README's summary table names the
job for each one precisely so that the citation is the job and not the table.

⚠ **And our file is Q2 while the issue's headline is a Q4-to-ternary path.** Our numbers bound a
different and smaller claim. `../wiki/04-the-byte-layout-and-the-engram-ceiling.md` says so in its
own words.

## What is NOT in here

⛔ **No measurement of ternary anything.** We have measured nothing about ternary retention on any
MoE. **Nothing in this folder is evidence that ternary works here**; it is evidence about the object
and about how to measure it.
